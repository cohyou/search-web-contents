'use babel';

import SearchWebContentsView from '../lib/search-web-contents-view';

describe('SearchWebContentsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
